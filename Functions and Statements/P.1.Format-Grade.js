function solve(gradeExam) {
    const gradesToCompare = [3.00, 3.00, 3.50, 4.50, 5.5];

    let validateGrade = ChekGradeFromExam(gradeExam, gradesToCompare);
}

ChekGradeFromExam(gradeExam, gradesToCompare) {

    for (const grade of gradesToCompare) {
        if (gradeExam === grade) {
            
        }
    }

}

solve('3.33');

